source [find interface/stlink.cfg]
transport select hla_swd

# Keep SWD slow and hold NRST during target examination. This helps when the
# running firmware, power state, or clock setup prevents normal hot attach.
adapter speed 1000
reset_config srst_only srst_nogate connect_assert_srst

source [find target/stm32g4x.cfg]
